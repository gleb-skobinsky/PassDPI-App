# The main C header you want to expose
headers = gitsubmodules/hevsockstunnel/include/hev-socks5-tunnel.h

# Optional header filter (limits which symbols get included)
headerFilter = gitsubmodules/hevsockstunnel/include/*

# Include paths for headers (must point to submodule directory)
compilerOpts = -Igitsubmodules/hevsockstunnel/include

# Linker options (if you link to a static or shared library)
# For example, if you build libhev_socks5_tunnel.a or .so/.dylib
linkerOpts = -Lgitsubmodules/include/build -lhev_socks5_tunnel
